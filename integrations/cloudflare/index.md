# Cloudflare integration bundle

Machine-readable [manifest](manifest.yaml), [governed sources and zero-gap ledger](sources.yaml), [capabilities](capabilities.yaml), [data semantics](data-semantics.yaml), [workflows](workflows.yaml), and [evaluations](evaluations.yaml) for `PLAYBOOK-CLOUDFLARE`.
